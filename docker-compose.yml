version: "3"
services:

  client:
    build: .
    networks:
      - habitica
    environment:
      - BASE_URL=http://server:7300
    ports:
        - "7070:8080"
    command: ["npm", "run", "client:dev"]
    depends_on:
      - server

  server:
    build: .
    ports:
      - "7300:3000"
    networks:
      - habitica
    environment:
      - NODE_DB_URI=mongodb://mongo/habitrpg
    depends_on:
      - mongo

  mongo:
    image: mongo:3.6
    ports:
      - "27017:27017"
    networks:
      - habitica

networks:
  habitica:
    driver: bridge
